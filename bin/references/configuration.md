# Bin Configuration

Configuration for bin utility scripts.

## Default Settings

- Python version: 3.8+
- Encoding: UTF-8
- Output: JSON structured format

## Customization

Override defaults via environment variables:
```bash
export BIN_OUTPUT_FORMAT=json
export BIN_LOG_LEVEL=info
```
